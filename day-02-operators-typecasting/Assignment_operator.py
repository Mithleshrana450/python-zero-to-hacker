# Create a variable score = 500.
# Using only assignment operators (+=, -=, *=), make it reach exactly 750.
# Print after each step.
score = 500
score += 400
score -= 400
score *= 1.5
print("Score reached to ", score)